require("which-key").setup({
  preset = "modern",
  delay = 3000,
  plugins = {
    presets = {
      text_objects = false,
      motions = false,
      operators = false,
      nav = false,
    },
  },
})
